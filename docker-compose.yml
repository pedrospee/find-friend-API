services:
  findafriend-pg:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=findafriend
    volumes:
      - findafriend-pg-data:/var/lib/postgresql/data

volumes:
  findafriend-pg-data:
